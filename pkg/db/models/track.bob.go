// Code generated by BobGen psql v0.49.0. DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"fmt"
	"io"
	"strings"

	"github.com/aarondl/opt/null"
	"github.com/aarondl/opt/omit"
	mytypes "github.com/mpapenbr/iracelog-service-manager-go/pkg/db/mytypes"
	"github.com/shopspring/decimal"
	"github.com/stephenafamo/bob"
	"github.com/stephenafamo/bob/dialect/psql"
	"github.com/stephenafamo/bob/dialect/psql/dialect"
	"github.com/stephenafamo/bob/dialect/psql/dm"
	"github.com/stephenafamo/bob/dialect/psql/sm"
	"github.com/stephenafamo/bob/dialect/psql/um"
	"github.com/stephenafamo/bob/expr"
	"github.com/stephenafamo/bob/mods"
	"github.com/stephenafamo/bob/orm"
	"github.com/stephenafamo/bob/types/pgtypes"
	"github.com/stephenafamo/scan"
)

// Track is an object representing the database table.
type Track struct {
	ID            int32               `db:"id,pk" `
	Name          string              `db:"name" `
	ShortName     string              `db:"short_name" `
	Config        string              `db:"config" `
	TrackLength   decimal.Decimal     `db:"track_length" `
	Sectors       mytypes.SectorSlice `db:"sectors" `
	PitSpeed      decimal.Decimal     `db:"pit_speed" `
	PitEntry      decimal.Decimal     `db:"pit_entry" `
	PitExit       decimal.Decimal     `db:"pit_exit" `
	PitLaneLength decimal.Decimal     `db:"pit_lane_length" `

	R trackR `db:"-" `

	C trackC `db:"-" `
}

// TrackSlice is an alias for a slice of pointers to Track.
// This should almost always be used instead of []*Track.
type TrackSlice []*Track

// Tracks contains methods to work with the track table
var Tracks = psql.NewTablex[*Track, TrackSlice, *TrackSetter]("", "track", buildTrackColumns("track"), trackScanMapper)

// TracksQuery is a query on the track table
type TracksQuery = *psql.ViewQuery[*Track, TrackSlice]

// trackR is where relationships are stored.
type trackR struct {
	Events EventSlice // event.event_track_id_fkey
	// Loaded reports whether each relationship has been loaded.
	// A relationship's bool is set by Load*, Preload, ThenLoad, factory builds,
	// and to-one Attach/Insert operations. To-many Attach/Insert operations leave it unchanged.
	Loaded trackRLoaded `db:"-" `
}

// trackRLoaded tracks which relationships on Track have been loaded.
type trackRLoaded struct {
	Events bool // event.event_track_id_fkey
}

func buildTrackColumns(tableName string) trackColumns {
	columnsExpr := expr.NewColumnsExpr(
		"id", "name", "short_name", "config", "track_length", "sectors", "pit_speed", "pit_entry", "pit_exit", "pit_lane_length",
	)

	if tableName != "" {
		columnsExpr = columnsExpr.WithParent(tableName)
	}

	return trackColumns{
		ColumnsExpr:   columnsExpr,
		tableAlias:    tableName,
		ID:            buildTrackColumn(tableName, "id"),
		Name:          buildTrackColumn(tableName, "name"),
		ShortName:     buildTrackColumn(tableName, "short_name"),
		Config:        buildTrackColumn(tableName, "config"),
		TrackLength:   buildTrackColumn(tableName, "track_length"),
		Sectors:       buildTrackColumn(tableName, "sectors"),
		PitSpeed:      buildTrackColumn(tableName, "pit_speed"),
		PitEntry:      buildTrackColumn(tableName, "pit_entry"),
		PitExit:       buildTrackColumn(tableName, "pit_exit"),
		PitLaneLength: buildTrackColumn(tableName, "pit_lane_length"),
	}
}

type trackColumns struct {
	expr.ColumnsExpr
	tableAlias    string
	ID            trackColumn
	Name          trackColumn
	ShortName     trackColumn
	Config        trackColumn
	TrackLength   trackColumn
	Sectors       trackColumn
	PitSpeed      trackColumn
	PitEntry      trackColumn
	PitExit       trackColumn
	PitLaneLength trackColumn
}

// Alias returns the current table alias for the columns set.
func (c trackColumns) Alias() string {
	return c.tableAlias
}

// AliasedAs returns a copy of the columns set qualified by tableName.
func (trackColumns) AliasedAs(tableName string) trackColumns {
	return buildTrackColumns(tableName)
}

// Unqualified returns a copy of the columns set without table qualification.
func (c trackColumns) Unqualified() trackColumns {
	return buildTrackColumns("")
}

func buildTrackColumn(alias, name string) trackColumn {
	return trackColumn{
		Expression: psql.Quote(alias, name),
		alias:      alias,
		name:       name,
	}
}

type trackColumn struct {
	psql.Expression
	alias string
	name  string
}

// Name returns the unqualified column name.
func (c trackColumn) Name() string {
	return c.name
}

// ShouldOmitParens prevents automatic parenthesis wrapping in expression builders.
func (c trackColumn) ShouldOmitParens() bool {
	return true
}

// TrackSetter is used for insert/upsert/update operations
// All values are optional, and do not have to be set
// Generated columns are not included
type TrackSetter struct {
	ID            omit.Val[int32]               `db:"id,pk" `
	Name          omit.Val[string]              `db:"name" `
	ShortName     omit.Val[string]              `db:"short_name" `
	Config        omit.Val[string]              `db:"config" `
	TrackLength   omit.Val[decimal.Decimal]     `db:"track_length" `
	Sectors       omit.Val[mytypes.SectorSlice] `db:"sectors" `
	PitSpeed      omit.Val[decimal.Decimal]     `db:"pit_speed" `
	PitEntry      omit.Val[decimal.Decimal]     `db:"pit_entry" `
	PitExit       omit.Val[decimal.Decimal]     `db:"pit_exit" `
	PitLaneLength omit.Val[decimal.Decimal]     `db:"pit_lane_length" `
}

func (s TrackSetter) SetColumns() []string {
	vals := make([]string, 0, 10)
	if s.ID.IsValue() {
		vals = append(vals, "id")
	}
	if s.Name.IsValue() {
		vals = append(vals, "name")
	}
	if s.ShortName.IsValue() {
		vals = append(vals, "short_name")
	}
	if s.Config.IsValue() {
		vals = append(vals, "config")
	}
	if s.TrackLength.IsValue() {
		vals = append(vals, "track_length")
	}
	if s.Sectors.IsValue() {
		vals = append(vals, "sectors")
	}
	if s.PitSpeed.IsValue() {
		vals = append(vals, "pit_speed")
	}
	if s.PitEntry.IsValue() {
		vals = append(vals, "pit_entry")
	}
	if s.PitExit.IsValue() {
		vals = append(vals, "pit_exit")
	}
	if s.PitLaneLength.IsValue() {
		vals = append(vals, "pit_lane_length")
	}
	return vals
}

func (s TrackSetter) Overwrite(t *Track) {
	if s.ID.IsValue() {
		t.ID = s.ID.MustGet()
	}
	if s.Name.IsValue() {
		t.Name = s.Name.MustGet()
	}
	if s.ShortName.IsValue() {
		t.ShortName = s.ShortName.MustGet()
	}
	if s.Config.IsValue() {
		t.Config = s.Config.MustGet()
	}
	if s.TrackLength.IsValue() {
		t.TrackLength = s.TrackLength.MustGet()
	}
	if s.Sectors.IsValue() {
		t.Sectors = s.Sectors.MustGet()
	}
	if s.PitSpeed.IsValue() {
		t.PitSpeed = s.PitSpeed.MustGet()
	}
	if s.PitEntry.IsValue() {
		t.PitEntry = s.PitEntry.MustGet()
	}
	if s.PitExit.IsValue() {
		t.PitExit = s.PitExit.MustGet()
	}
	if s.PitLaneLength.IsValue() {
		t.PitLaneLength = s.PitLaneLength.MustGet()
	}
}

func (s *TrackSetter) Apply(q *dialect.InsertQuery) {
	q.AppendHooks(func(ctx context.Context, exec bob.Executor) (context.Context, error) {
		return Tracks.BeforeInsertHooks.RunHooks(ctx, exec, s)
	})

	q.AppendValues(
		bob.ExpressionFunc(func(ctx context.Context, w io.StringWriter, d bob.Dialect, start int) ([]any, error) {
			if s.ID.IsUnset() {
				return psql.Raw("DEFAULT").WriteSQL(ctx, w, d, start)
			}
			return psql.Arg(s.ID.MustGet()).WriteSQL(ctx, w, d, start)
		}), bob.ExpressionFunc(func(ctx context.Context, w io.StringWriter, d bob.Dialect, start int) ([]any, error) {
			if s.Name.IsUnset() {
				return psql.Raw("DEFAULT").WriteSQL(ctx, w, d, start)
			}
			return psql.Arg(s.Name.MustGet()).WriteSQL(ctx, w, d, start)
		}), bob.ExpressionFunc(func(ctx context.Context, w io.StringWriter, d bob.Dialect, start int) ([]any, error) {
			if s.ShortName.IsUnset() {
				return psql.Raw("DEFAULT").WriteSQL(ctx, w, d, start)
			}
			return psql.Arg(s.ShortName.MustGet()).WriteSQL(ctx, w, d, start)
		}), bob.ExpressionFunc(func(ctx context.Context, w io.StringWriter, d bob.Dialect, start int) ([]any, error) {
			if s.Config.IsUnset() {
				return psql.Raw("DEFAULT").WriteSQL(ctx, w, d, start)
			}
			return psql.Arg(s.Config.MustGet()).WriteSQL(ctx, w, d, start)
		}), bob.ExpressionFunc(func(ctx context.Context, w io.StringWriter, d bob.Dialect, start int) ([]any, error) {
			if s.TrackLength.IsUnset() {
				return psql.Raw("DEFAULT").WriteSQL(ctx, w, d, start)
			}
			return psql.Arg(s.TrackLength.MustGet()).WriteSQL(ctx, w, d, start)
		}), bob.ExpressionFunc(func(ctx context.Context, w io.StringWriter, d bob.Dialect, start int) ([]any, error) {
			if s.Sectors.IsUnset() {
				return psql.Raw("DEFAULT").WriteSQL(ctx, w, d, start)
			}
			return psql.Arg(s.Sectors.MustGet()).WriteSQL(ctx, w, d, start)
		}), bob.ExpressionFunc(func(ctx context.Context, w io.StringWriter, d bob.Dialect, start int) ([]any, error) {
			if s.PitSpeed.IsUnset() {
				return psql.Raw("DEFAULT").WriteSQL(ctx, w, d, start)
			}
			return psql.Arg(s.PitSpeed.MustGet()).WriteSQL(ctx, w, d, start)
		}), bob.ExpressionFunc(func(ctx context.Context, w io.StringWriter, d bob.Dialect, start int) ([]any, error) {
			if s.PitEntry.IsUnset() {
				return psql.Raw("DEFAULT").WriteSQL(ctx, w, d, start)
			}
			return psql.Arg(s.PitEntry.MustGet()).WriteSQL(ctx, w, d, start)
		}), bob.ExpressionFunc(func(ctx context.Context, w io.StringWriter, d bob.Dialect, start int) ([]any, error) {
			if s.PitExit.IsUnset() {
				return psql.Raw("DEFAULT").WriteSQL(ctx, w, d, start)
			}
			return psql.Arg(s.PitExit.MustGet()).WriteSQL(ctx, w, d, start)
		}), bob.ExpressionFunc(func(ctx context.Context, w io.StringWriter, d bob.Dialect, start int) ([]any, error) {
			if s.PitLaneLength.IsUnset() {
				return psql.Raw("DEFAULT").WriteSQL(ctx, w, d, start)
			}
			return psql.Arg(s.PitLaneLength.MustGet()).WriteSQL(ctx, w, d, start)
		}))
}

func (s TrackSetter) UpdateMod() bob.Mod[*dialect.UpdateQuery] {
	return um.Set(s.Expressions()...)
}

func (s TrackSetter) Expressions(prefix ...string) []bob.Expression {
	exprs := make([]bob.Expression, 0, 10)

	if s.ID.IsValue() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			psql.Quote(append(prefix, "id")...),
			psql.Arg(s.ID),
		}})
	}

	if s.Name.IsValue() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			psql.Quote(append(prefix, "name")...),
			psql.Arg(s.Name),
		}})
	}

	if s.ShortName.IsValue() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			psql.Quote(append(prefix, "short_name")...),
			psql.Arg(s.ShortName),
		}})
	}

	if s.Config.IsValue() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			psql.Quote(append(prefix, "config")...),
			psql.Arg(s.Config),
		}})
	}

	if s.TrackLength.IsValue() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			psql.Quote(append(prefix, "track_length")...),
			psql.Arg(s.TrackLength),
		}})
	}

	if s.Sectors.IsValue() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			psql.Quote(append(prefix, "sectors")...),
			psql.Arg(s.Sectors),
		}})
	}

	if s.PitSpeed.IsValue() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			psql.Quote(append(prefix, "pit_speed")...),
			psql.Arg(s.PitSpeed),
		}})
	}

	if s.PitEntry.IsValue() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			psql.Quote(append(prefix, "pit_entry")...),
			psql.Arg(s.PitEntry),
		}})
	}

	if s.PitExit.IsValue() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			psql.Quote(append(prefix, "pit_exit")...),
			psql.Arg(s.PitExit),
		}})
	}

	if s.PitLaneLength.IsValue() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			psql.Quote(append(prefix, "pit_lane_length")...),
			psql.Arg(s.PitLaneLength),
		}})
	}

	return exprs
}

// scan.Mapper[*T] without reflection: maps columns to fields by index.
func trackScanMapper(ctx context.Context, cols []string) (scan.BeforeFunc, func(any) (*Track, error)) {
	// resolve the column positions once per query, not per row
	type target struct {
		idx int
		dst func(o *Track) any
	}
	targets := make([]target, 0, 10)
	for i, col := range cols {
		switch col {
		case "id":
			targets = append(targets, target{i, func(o *Track) any { return &o.ID }})
		case "name":
			targets = append(targets, target{i, func(o *Track) any { return &o.Name }})
		case "short_name":
			targets = append(targets, target{i, func(o *Track) any { return &o.ShortName }})
		case "config":
			targets = append(targets, target{i, func(o *Track) any { return &o.Config }})
		case "track_length":
			targets = append(targets, target{i, func(o *Track) any { return &o.TrackLength }})
		case "sectors":
			targets = append(targets, target{i, func(o *Track) any { return &o.Sectors }})
		case "pit_speed":
			targets = append(targets, target{i, func(o *Track) any { return &o.PitSpeed }})
		case "pit_entry":
			targets = append(targets, target{i, func(o *Track) any { return &o.PitEntry }})
		case "pit_exit":
			targets = append(targets, target{i, func(o *Track) any { return &o.PitExit }})
		case "pit_lane_length":
			targets = append(targets, target{i, func(o *Track) any { return &o.PitLaneLength }})
		}
	}

	return func(row *scan.Row) (any, error) {
			o := new(Track)
			for _, t := range targets {
				row.ScheduleScanByIndex(t.idx, t.dst(o))
			}
			return o, nil
		}, func(v any) (*Track, error) {
			return v.(*Track), nil
		}
}

// FindTrack retrieves a single record by primary key
// If cols is empty Find will return all columns.
func FindTrack(ctx context.Context, exec bob.Executor, IDPK int32, cols ...string) (*Track, error) {
	if len(cols) == 0 {
		return Tracks.Query(
			sm.Where(Tracks.Columns.ID.EQ(psql.Arg(IDPK))),
		).One(ctx, exec)
	}

	return Tracks.Query(
		sm.Where(Tracks.Columns.ID.EQ(psql.Arg(IDPK))),
		sm.Columns(Tracks.Columns.Only(cols...)),
	).One(ctx, exec)
}

// TrackExists checks the presence of a single record by primary key
func TrackExists(ctx context.Context, exec bob.Executor, IDPK int32) (bool, error) {
	return Tracks.Query(
		sm.Where(Tracks.Columns.ID.EQ(psql.Arg(IDPK))),
	).Exists(ctx, exec)
}

// AfterQueryHook is called after Track is retrieved from the database
func (o *Track) AfterQueryHook(ctx context.Context, exec bob.Executor, queryType bob.QueryType) error {
	var err error

	switch queryType {
	case bob.QueryTypeSelect:
		ctx, err = Tracks.AfterSelectHooks.RunHooks(ctx, exec, TrackSlice{o})
	case bob.QueryTypeInsert:
		ctx, err = Tracks.AfterInsertHooks.RunHooks(ctx, exec, TrackSlice{o})
	case bob.QueryTypeUpdate:
		ctx, err = Tracks.AfterUpdateHooks.RunHooks(ctx, exec, TrackSlice{o})
	case bob.QueryTypeDelete:
		ctx, err = Tracks.AfterDeleteHooks.RunHooks(ctx, exec, TrackSlice{o})
	case bob.QueryTypeMerge:
		ctx, err = Tracks.AfterMergeHooks.RunHooks(ctx, exec, TrackSlice{o})
	}

	return err
}

// primaryKeyVals returns the primary key values of the Track
func (o *Track) primaryKeyVals() bob.Expression {
	return psql.Arg(o.ID)
}

func (o *Track) pkEQ() dialect.Expression {
	return psql.Quote("track", "id").EQ(bob.ExpressionFunc(func(ctx context.Context, w io.StringWriter, d bob.Dialect, start int) ([]any, error) {
		return o.primaryKeyVals().WriteSQL(ctx, w, d, start)
	}))
}

// Update uses an executor to update the Track
func (o *Track) Update(ctx context.Context, exec bob.Executor, s *TrackSetter) error {
	v, err := Tracks.Update(s.UpdateMod(), um.Where(o.pkEQ())).One(ctx, exec)
	if err != nil {
		return err
	}

	o.R = v.R
	*o = *v

	return nil
}

// Delete deletes a single Track record with an executor
func (o *Track) Delete(ctx context.Context, exec bob.Executor) error {
	_, err := Tracks.Delete(dm.Where(o.pkEQ())).Exec(ctx, exec)
	return err
}

// Reload refreshes the Track using the executor
func (o *Track) Reload(ctx context.Context, exec bob.Executor) error {
	o2, err := Tracks.Query(
		sm.Where(Tracks.Columns.ID.EQ(psql.Arg(o.ID))),
	).One(ctx, exec)
	if err != nil {
		return err
	}
	o2.R = o.R
	*o = *o2

	return nil
}

// AfterQueryHook is called after TrackSlice is retrieved from the database
func (o TrackSlice) AfterQueryHook(ctx context.Context, exec bob.Executor, queryType bob.QueryType) error {
	var err error

	switch queryType {
	case bob.QueryTypeSelect:
		ctx, err = Tracks.AfterSelectHooks.RunHooks(ctx, exec, o)
	case bob.QueryTypeInsert:
		ctx, err = Tracks.AfterInsertHooks.RunHooks(ctx, exec, o)
	case bob.QueryTypeUpdate:
		ctx, err = Tracks.AfterUpdateHooks.RunHooks(ctx, exec, o)
	case bob.QueryTypeDelete:
		ctx, err = Tracks.AfterDeleteHooks.RunHooks(ctx, exec, o)
	case bob.QueryTypeMerge:
		ctx, err = Tracks.AfterMergeHooks.RunHooks(ctx, exec, o)
	}

	return err
}

func (o TrackSlice) pkIN() dialect.Expression {
	if len(o) == 0 {
		return psql.Raw("NULL")
	}

	return psql.Quote("track", "id").In(bob.ExpressionFunc(func(ctx context.Context, w io.StringWriter, d bob.Dialect, start int) ([]any, error) {
		pkPairs := make([]bob.Expression, len(o))
		for i, row := range o {
			pkPairs[i] = row.primaryKeyVals()
		}
		return bob.ExpressSlice(ctx, w, d, start, pkPairs, "", ", ", "")
	}))
}

// copyMatchingRows finds models in the given slice that have the same primary key
// then it first copies the existing relationships from the old model to the new model
// and then replaces the old model in the slice with the new model
func (o TrackSlice) copyMatchingRows(from ...*Track) {
	fromByPK := make(map[int32]*Track, len(from))
	for _, new := range from {
		// keep the first row for each key, like the nested loop did
		if _, ok := fromByPK[new.ID]; !ok {
			fromByPK[new.ID] = new
		}
	}

	for i, old := range o {
		new, ok := fromByPK[old.ID]
		if !ok {
			continue
		}
		new.R = old.R
		o[i] = new
	}
}

// UpdateMod modifies an update query with "WHERE primary_key IN (o...)"
func (o TrackSlice) UpdateMod() bob.Mod[*dialect.UpdateQuery] {
	return bob.ModFunc[*dialect.UpdateQuery](func(q *dialect.UpdateQuery) {
		q.AppendHooks(func(ctx context.Context, exec bob.Executor) (context.Context, error) {
			return Tracks.BeforeUpdateHooks.RunHooks(ctx, exec, o)
		})

		q.AppendLoader(bob.LoaderFunc(func(ctx context.Context, exec bob.Executor, retrieved any) error {
			var err error
			switch retrieved := retrieved.(type) {
			case *Track:
				o.copyMatchingRows(retrieved)
			case []*Track:
				o.copyMatchingRows(retrieved...)
			case TrackSlice:
				o.copyMatchingRows(retrieved...)
			default:
				// If the retrieved value is not a Track or a slice of Track
				// then run the AfterUpdateHooks on the slice
				_, err = Tracks.AfterUpdateHooks.RunHooks(ctx, exec, o)
			}

			return err
		}))

		q.AppendWhere(o.pkIN())
	})
}

// DeleteMod modifies an delete query with "WHERE primary_key IN (o...)"
func (o TrackSlice) DeleteMod() bob.Mod[*dialect.DeleteQuery] {
	return bob.ModFunc[*dialect.DeleteQuery](func(q *dialect.DeleteQuery) {
		q.AppendHooks(func(ctx context.Context, exec bob.Executor) (context.Context, error) {
			return Tracks.BeforeDeleteHooks.RunHooks(ctx, exec, o)
		})

		q.AppendLoader(bob.LoaderFunc(func(ctx context.Context, exec bob.Executor, retrieved any) error {
			var err error
			switch retrieved := retrieved.(type) {
			case *Track:
				o.copyMatchingRows(retrieved)
			case []*Track:
				o.copyMatchingRows(retrieved...)
			case TrackSlice:
				o.copyMatchingRows(retrieved...)
			default:
				// If the retrieved value is not a Track or a slice of Track
				// then run the AfterDeleteHooks on the slice
				_, err = Tracks.AfterDeleteHooks.RunHooks(ctx, exec, o)
			}

			return err
		}))

		q.AppendWhere(o.pkIN())
	})
}

// MergeMod modifies a merge query to run BeforeMergeHooks and AfterMergeHooks
// and updates the slice with the returned rows.
func (o TrackSlice) MergeMod() bob.Mod[*dialect.MergeQuery] {
	return bob.ModFunc[*dialect.MergeQuery](func(q *dialect.MergeQuery) {
		q.AppendHooks(func(ctx context.Context, exec bob.Executor) (context.Context, error) {
			return Tracks.BeforeMergeHooks.RunHooks(ctx, exec, o)
		})

		q.AppendLoader(bob.LoaderFunc(func(ctx context.Context, exec bob.Executor, retrieved any) error {
			var err error
			switch retrieved := retrieved.(type) {
			case *Track:
				o.copyMatchingRows(retrieved)
			case []*Track:
				o.copyMatchingRows(retrieved...)
			case TrackSlice:
				o.copyMatchingRows(retrieved...)
			default:
				// If the retrieved value is not a Track or a slice of Track
				// then run the AfterMergeHooks on the slice
				_, err = Tracks.AfterMergeHooks.RunHooks(ctx, exec, o)
			}

			return err
		}))
	})
}

func (o TrackSlice) UpdateAll(ctx context.Context, exec bob.Executor, vals TrackSetter) error {
	if len(o) == 0 {
		return nil
	}

	_, err := Tracks.Update(vals.UpdateMod(), o.UpdateMod()).All(ctx, exec)
	return err
}

func (o TrackSlice) DeleteAll(ctx context.Context, exec bob.Executor) error {
	if len(o) == 0 {
		return nil
	}

	_, err := Tracks.Delete(o.DeleteMod()).Exec(ctx, exec)
	return err
}

func (o TrackSlice) ReloadAll(ctx context.Context, exec bob.Executor) error {
	if len(o) == 0 {
		return nil
	}

	o2, err := Tracks.Query(sm.Where(o.pkIN())).All(ctx, exec)
	if err != nil {
		return err
	}

	o.copyMatchingRows(o2...)

	return nil
}

// Events starts a query for related objects on event
func (o *Track) Events(mods ...bob.Mod[*dialect.SelectQuery]) EventsQuery {
	return Events.Query(append(mods,
		sm.Where(Events.Columns.TrackID.EQ(psql.Arg(o.ID))),
	)...)
}

func (os TrackSlice) Events(mods ...bob.Mod[*dialect.SelectQuery]) EventsQuery {
	pkID := make(pgtypes.Array[int32], 0, len(os))
	for _, o := range os {
		if o == nil {
			continue
		}
		pkID = append(pkID, o.ID)
	}

	PKArgExpr := psql.Any(psql.Cast(psql.Arg(pkID), "integer[]"))

	return Events.Query(append(mods,
		sm.Where(Events.Columns.TrackID.EQ(PKArgExpr)),
	)...)
}

func insertTrackEvents0(ctx context.Context, exec bob.Executor, events1 []*EventSetter, track0 *Track) (EventSlice, error) {
	for i := range events1 {
		events1[i].TrackID = omit.From(track0.ID)
	}

	ret, err := Events.Insert(bob.ToMods(events1...)).All(ctx, exec)
	if err != nil {
		return ret, fmt.Errorf("insertTrackEvents0: %w", err)
	}

	return ret, nil
}

func attachTrackEvents0(ctx context.Context, exec bob.Executor, count int, events1 EventSlice, track0 *Track) (EventSlice, error) {
	setter := &EventSetter{
		TrackID: omit.From(track0.ID),
	}

	err := events1.UpdateAll(ctx, exec, *setter)
	if err != nil {
		return nil, fmt.Errorf("attachTrackEvents0: %w", err)
	}

	return events1, nil
}

func (track0 *Track) InsertEvents(ctx context.Context, exec bob.Executor, related ...*EventSetter) error {
	if len(related) == 0 {
		return nil
	}

	var err error

	events1, err := insertTrackEvents0(ctx, exec, related, track0)
	if err != nil {
		return err
	}

	track0.R.Events = append(track0.R.Events, events1...)

	for _, rel := range events1 {
		rel.R.Track = track0
		rel.R.Loaded.Track = true
	}
	return nil
}

func (track0 *Track) AttachEvents(ctx context.Context, exec bob.Executor, related ...*Event) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	events1 := EventSlice(related)

	_, err = attachTrackEvents0(ctx, exec, len(related), events1, track0)
	if err != nil {
		return err
	}

	track0.R.Events = append(track0.R.Events, events1...)

	for _, rel := range related {
		rel.R.Track = track0
		rel.R.Loaded.Track = true
	}

	return nil
}

type trackWhere[Q psql.Filterable] struct {
	cols          trackColumns
	ID            psql.WhereMod[Q, int32]
	Name          psql.WhereMod[Q, string]
	ShortName     psql.WhereMod[Q, string]
	Config        psql.WhereMod[Q, string]
	TrackLength   psql.WhereMod[Q, decimal.Decimal]
	Sectors       psql.WhereMod[Q, mytypes.SectorSlice]
	PitSpeed      psql.WhereMod[Q, decimal.Decimal]
	PitEntry      psql.WhereMod[Q, decimal.Decimal]
	PitExit       psql.WhereMod[Q, decimal.Decimal]
	PitLaneLength psql.WhereMod[Q, decimal.Decimal]
	R             trackWhereR[Q]
}

func (trackWhere[Q]) AliasedAs(alias string) trackWhere[Q] {
	return buildTrackWhere[Q](buildTrackColumns(alias))
}

func buildTrackWhere[Q psql.Filterable](cols trackColumns) trackWhere[Q] {
	return trackWhere[Q]{
		cols:          cols,
		ID:            psql.Where[Q, int32](cols.ID.Expression),
		Name:          psql.Where[Q, string](cols.Name.Expression),
		ShortName:     psql.Where[Q, string](cols.ShortName.Expression),
		Config:        psql.Where[Q, string](cols.Config.Expression),
		TrackLength:   psql.Where[Q, decimal.Decimal](cols.TrackLength.Expression),
		Sectors:       psql.Where[Q, mytypes.SectorSlice](cols.Sectors.Expression),
		PitSpeed:      psql.Where[Q, decimal.Decimal](cols.PitSpeed.Expression),
		PitEntry:      psql.Where[Q, decimal.Decimal](cols.PitEntry.Expression),
		PitExit:       psql.Where[Q, decimal.Decimal](cols.PitExit.Expression),
		PitLaneLength: psql.Where[Q, decimal.Decimal](cols.PitLaneLength.Expression),
		R:             trackWhereR[Q]{cols: cols},
	}
}

// trackWhereR holds the relationship-based filters of
// trackWhere under the R namespace — mirroring the model's
// R struct — so they cannot collide with column-based filter fields.
type trackWhereR[Q psql.Filterable] struct {
	cols trackColumns
}

// HasEvents filters parents that have a matching Events using a
// correlated EXISTS subquery (semi-join). Unlike an INNER JOIN it does not
// multiply parent rows, so no DISTINCT is needed. The optional filters are
// applied to the subquery (i.e. to Events).
func (w trackWhereR[Q]) HasEvents(filters ...bob.Mod[*dialect.SelectQuery]) mods.Where[Q] {
	q := psql.Select(
		sm.Columns(psql.Raw("1")),
		sm.From(Events.NameExpr()),
		sm.Where(Events.Columns.TrackID.EQ(w.cols.ID)),
	)
	q.Apply(filters...)
	return mods.Where[Q]{E: psql.Exists(q)}
}

// NULL-tolerant scan buffer for trackScanMapperNullable:
// on a LEFT JOIN miss every column comes back NULL, so each field uses the
// nullable version of the column type even when the column itself is NOT NULL.
type trackPreloadBuf struct {
	ID            null.Val[int32]
	Name          null.Val[string]
	ShortName     null.Val[string]
	Config        null.Val[string]
	TrackLength   null.Val[decimal.Decimal]
	Sectors       null.Val[mytypes.SectorSlice]
	PitSpeed      null.Val[decimal.Decimal]
	PitEntry      null.Val[decimal.Decimal]
	PitExit       null.Val[decimal.Decimal]
	PitLaneLength null.Val[decimal.Decimal]
}

// trackScanMapperNullable maps the preloaded track
// columns (prefixed with the runtime join alias) without reflection, while
// keeping the LEFT JOIN semantics of the reflection-based mapper: a row whose
// prefixed columns are all NULL yields nil (no child), and NULL values never
// error, they just leave the zero value in the field.
func trackScanMapperNullable(prefix string) scan.Mapper[*Track] {
	return func(ctx context.Context, cols []string) (scan.BeforeFunc, func(any) (*Track, error)) {
		// resolve the column names once per query, not per row
		type target struct {
			idx int
			dst func(b *trackPreloadBuf) any
		}
		targets := make([]target, 0, 10)
		for i, col := range cols {
			name, ok := strings.CutPrefix(col, prefix)
			if !ok {
				continue
			}
			switch name {
			case "id":
				targets = append(targets, target{i, func(b *trackPreloadBuf) any { return &b.ID }})
			case "name":
				targets = append(targets, target{i, func(b *trackPreloadBuf) any { return &b.Name }})
			case "short_name":
				targets = append(targets, target{i, func(b *trackPreloadBuf) any { return &b.ShortName }})
			case "config":
				targets = append(targets, target{i, func(b *trackPreloadBuf) any { return &b.Config }})
			case "track_length":
				targets = append(targets, target{i, func(b *trackPreloadBuf) any { return &b.TrackLength }})
			case "sectors":
				targets = append(targets, target{i, func(b *trackPreloadBuf) any { return &b.Sectors }})
			case "pit_speed":
				targets = append(targets, target{i, func(b *trackPreloadBuf) any { return &b.PitSpeed }})
			case "pit_entry":
				targets = append(targets, target{i, func(b *trackPreloadBuf) any { return &b.PitEntry }})
			case "pit_exit":
				targets = append(targets, target{i, func(b *trackPreloadBuf) any { return &b.PitExit }})
			case "pit_lane_length":
				targets = append(targets, target{i, func(b *trackPreloadBuf) any { return &b.PitLaneLength }})
			}
		}

		// One scratch buffer reused for every row: before -> scan -> after runs
		// synchronously per row, the scheduled columns are fully overwritten each
		// row, and unselected columns stay zero, so a single buffer preserves the
		// per-row NULL semantics without allocating a buffer per parent row. The
		// returned child (o below) is always freshly allocated, so buf never
		// escapes past the after func.
		buf := new(trackPreloadBuf)
		return func(row *scan.Row) (any, error) {
				for _, t := range targets {
					row.ScheduleScanByIndex(t.idx, t.dst(buf))
				}
				return buf, nil
			}, func(link any) (*Track, error) {
				buf := link.(*trackPreloadBuf)

				// Same rule as the reflection mapper's row validator: the child
				// exists only if at least one of its columns is not NULL. Columns
				// not selected by the query are never scanned and stay invalid,
				// so this check also matches when only a subset is selected.
				if !(buf.ID.IsValue()) &&
					!(buf.Name.IsValue()) &&
					!(buf.ShortName.IsValue()) &&
					!(buf.Config.IsValue()) &&
					!(buf.TrackLength.IsValue()) &&
					!(buf.Sectors.IsValue()) &&
					!(buf.PitSpeed.IsValue()) &&
					!(buf.PitEntry.IsValue()) &&
					!(buf.PitExit.IsValue()) &&
					!(buf.PitLaneLength.IsValue()) {
					return nil, nil
				}

				o := new(Track)
				if buf.ID.IsValue() {
					o.ID = buf.ID.MustGet()
				}
				if buf.Name.IsValue() {
					o.Name = buf.Name.MustGet()
				}
				if buf.ShortName.IsValue() {
					o.ShortName = buf.ShortName.MustGet()
				}
				if buf.Config.IsValue() {
					o.Config = buf.Config.MustGet()
				}
				if buf.TrackLength.IsValue() {
					o.TrackLength = buf.TrackLength.MustGet()
				}
				if buf.Sectors.IsValue() {
					o.Sectors = buf.Sectors.MustGet()
				}
				if buf.PitSpeed.IsValue() {
					o.PitSpeed = buf.PitSpeed.MustGet()
				}
				if buf.PitEntry.IsValue() {
					o.PitEntry = buf.PitEntry.MustGet()
				}
				if buf.PitExit.IsValue() {
					o.PitExit = buf.PitExit.MustGet()
				}
				if buf.PitLaneLength.IsValue() {
					o.PitLaneLength = buf.PitLaneLength.MustGet()
				}
				return o, nil
			}
	}
}

func (o *Track) Preload(name string, retrieved any) error {
	if o == nil {
		return nil
	}

	switch name {
	case "Events":
		rels, ok := retrieved.(EventSlice)
		if !ok {
			return fmt.Errorf("track cannot load %T as %q", retrieved, name)
		}

		o.R.Events = rels
		o.R.Loaded.Events = true

		for _, rel := range rels {
			if rel != nil {
				rel.R.Track = o
				rel.R.Loaded.Track = true
			}
		}
		return nil
	default:
		return fmt.Errorf("track has no relationship %q", name)
	}
}

type trackPreloader struct{}

func buildTrackPreloader() trackPreloader {
	return trackPreloader{}
}

type trackThenLoader[Q orm.Loadable] struct {
	Events func(...bob.Mod[*dialect.SelectQuery]) orm.Loader[Q]
}

func buildTrackThenLoader[Q orm.Loadable]() trackThenLoader[Q] {
	type EventsLoadInterface interface {
		LoadEvents(context.Context, bob.Executor, ...bob.Mod[*dialect.SelectQuery]) error
	}

	return trackThenLoader[Q]{
		Events: thenLoadBuilder[Q](
			"Events",
			func(ctx context.Context, exec bob.Executor, retrieved EventsLoadInterface, mods ...bob.Mod[*dialect.SelectQuery]) error {
				return retrieved.LoadEvents(ctx, exec, mods...)
			},
		),
	}
}

// LoadEvents loads the track's Events into the .R struct
func (o *Track) LoadEvents(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if o == nil {
		return nil
	}

	// Reset the relationship
	o.R.Events = nil
	o.R.Loaded.Events = false

	related, err := o.Events(mods...).All(ctx, exec)
	if err != nil {
		return err
	}

	for _, rel := range related {
		rel.R.Track = o
		rel.R.Loaded.Track = true
	}

	o.R.Events = related
	o.R.Loaded.Events = true
	return nil
}

// LoadEvents loads the track's Events into the .R struct
func (os TrackSlice) LoadEvents(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if len(os) == 0 {
		return nil
	}

	events, err := os.Events(mods...).All(ctx, exec)
	if err != nil {
		return err
	}

	for _, o := range os {
		if o == nil {
			continue
		}

		o.R.Events = nil
		o.R.Loaded.Events = true
	}
	// O(N+M) stitch via a map keyed by the join column (key -> []parent; was O(N*M)).
	trackByKey := make(map[int32][]*Track, len(os))
	for _, o := range os {
		if o == nil {
			continue
		}

		trackByKey[o.ID] = append(trackByKey[o.ID], o)
	}

	for _, rel := range events {

		owners, ok := trackByKey[rel.TrackID]
		if !ok {
			continue
		}

		for _, o := range owners {

			rel.R.Track = o
			rel.R.Loaded.Track = true

			o.R.Events = append(o.R.Events, rel)

		}
	}

	return nil
}

// trackC is where relationship counts are stored.
type trackC struct {
	Events *int64
}

// PreloadCount sets a count in the C struct by name
func (o *Track) PreloadCount(name string, count int64) error {
	if o == nil {
		return nil
	}

	switch name {
	case "Events":
		o.C.Events = &count
	}
	return nil
}

type trackCountPreloader struct {
	Events func(...bob.Mod[*dialect.SelectQuery]) psql.Preloader
}

func buildTrackCountPreloader() trackCountPreloader {
	return trackCountPreloader{
		Events: func(mods ...bob.Mod[*dialect.SelectQuery]) psql.Preloader {
			return countPreloader[*Track]("Events", func(parent string) bob.Expression {
				// Build a correlated subquery: (SELECT COUNT(*) FROM related WHERE fk = parent.pk)
				if parent == "" {
					parent = Tracks.Alias()
				}

				subqueryMods := []bob.Mod[*dialect.SelectQuery]{
					sm.Columns(psql.Raw("count(*)")),

					sm.From(Events.NameAsExpr()),
					sm.Where(psql.Quote(Events.Alias(), "track_id").EQ(psql.Quote(parent, "id"))),
				}
				subqueryMods = append(subqueryMods, mods...)
				return psql.Group(psql.Select(subqueryMods...).Expression)
			})
		},
	}
}

type trackCountThenLoader[Q orm.Loadable] struct {
	Events func(...bob.Mod[*dialect.SelectQuery]) orm.Loader[Q]
}

func buildTrackCountThenLoader[Q orm.Loadable]() trackCountThenLoader[Q] {
	type EventsCountInterface interface {
		LoadCountEvents(context.Context, bob.Executor, ...bob.Mod[*dialect.SelectQuery]) error
	}

	return trackCountThenLoader[Q]{
		Events: countThenLoadBuilder[Q](
			"Events",
			func(ctx context.Context, exec bob.Executor, retrieved EventsCountInterface, mods ...bob.Mod[*dialect.SelectQuery]) error {
				return retrieved.LoadCountEvents(ctx, exec, mods...)
			},
		),
	}
}

// LoadCountEvents loads the count of Events into the C struct
func (o *Track) LoadCountEvents(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if o == nil {
		return nil
	}

	count, err := o.Events(mods...).Count(ctx, exec)
	if err != nil {
		return err
	}

	o.C.Events = &count
	return nil
}

// LoadCountEvents loads the count of Events for a slice in a single batch query
func (os TrackSlice) LoadCountEvents(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if len(os) == 0 {
		return nil
	}

	// Build the IN arg expression from parent PKs

	pkID := make(pgtypes.Array[int32], 0, len(os))
	for _, o := range os {
		if o == nil {
			continue
		}
		pkID = append(pkID, o.ID)
	}
	PKArgExpr := psql.Any(psql.Cast(psql.Arg(pkID), "integer[]"))

	// countResult holds one scanned row from the batch count query.
	// FK columns are aliased to the parent PK column names for direct map lookup.
	type countResult struct {
		ID    int32
		Count int64
	}

	batchMods := []bob.Mod[*dialect.SelectQuery]{
		// SELECT fk AS parent_pk, count(*)
		sm.Columns(
			Events.Columns.TrackID.As("id"),
			psql.Raw("count(*) as count"),
		),
		// Single-hop: FROM related table directly
		sm.From(Events.NameAsExpr()),

		// WHERE fk IN (parent PKs) — psql single-column FK uses `= ANY(array)` (see PKArgExpr above)
		sm.Where(Events.Columns.TrackID.EQ(PKArgExpr)),
		// GROUP BY fk columns
		sm.GroupBy(Events.Columns.TrackID),
	}
	batchMods = append(batchMods, mods...)

	results, err := bob.All(ctx, exec,
		psql.Select(batchMods...),
		scan.StructMapper[countResult](),
	)
	if err != nil {
		return err
	}

	// Single-column FK: direct map lookup
	countMap := make(map[int32]int64, len(results))
	for _, r := range results {
		countMap[r.ID] = r.Count
	}
	for _, o := range os {
		if o == nil {
			continue
		}
		count := countMap[o.ID]
		o.C.Events = &count
	}

	return nil
}

type trackJoins[Q dialect.Joinable] struct {
	typ    string
	Events modAs[Q, eventColumns]
}

func (j trackJoins[Q]) aliasedAs(alias string) trackJoins[Q] {
	return buildTrackJoins[Q](buildTrackColumns(alias), j.typ)
}

func buildTrackJoins[Q dialect.Joinable](cols trackColumns, typ string) trackJoins[Q] {
	return trackJoins[Q]{
		typ: typ,
		Events: modAs[Q, eventColumns]{
			c: Events.Columns,
			f: func(to eventColumns) bob.Mod[Q] {
				mods := make(mods.QueryMods[Q], 0, 1)

				{
					mods = append(mods, dialect.Join[Q](typ, Events.NameExpr().As(to.Alias())).On(
						to.TrackID.EQ(cols.ID),
					))
				}

				return mods
			},
		},
	}
}
